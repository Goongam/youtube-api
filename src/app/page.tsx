import ViewContainer from "@/components/ViewContainer";

export default function Home() {
  /**TODO:
   * redux 적용
   * 1. 번역 버튼 -> 받아온 caption데이터
   * 2. 각 process에 따른
   */
  return <ViewContainer />;
}
